apiVersion: v1
kind: Secret
metadata:
  name: email-smtp-secret
  labels:
    app: ai-api-gateway
type: Opaque
stringData:
  SMTP_USERNAME: "user@example.com"
  SMTP_PASSWORD: "<APP_PASSWORD_OR_OAUTH_TOKEN>"
  SMTP_FROM: "user@example.com"
